## Substitution Permutation Solver
### Binary
This is a java program that runs in the console and encrypts 4-digit binary numbers using the Substitution-Permutation method. <br>
If you want to run it, make sure you have a jre/jvm version 11 or higher, then just run the batch file (run.bat) and the program will run in the command prompt.
